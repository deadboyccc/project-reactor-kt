org.gradle.logging.level = quiet
# Fixes the "Consider enabling configuration cache" warning
org.gradle.configuration-cache=true
# Reuses task outputs from previous builds (Build Cache)
org.gradle.caching=true
# Allows Gradle to run tasks in parallel (highly recommended for multi-core laptops)
org.gradle.parallel=true
